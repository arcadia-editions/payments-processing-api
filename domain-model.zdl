config {
    id "urn:com.arcadiaeditions.payments.processing"
    title "Arcadia Editions - Payments Processing"
    basePackage "com.arcadiaeditions.payments.processing"
    persistence jpa

    plugins {
        /** Generates an AsyncAPI v3 specification from this ZDL model. */
        ZDLToAsyncAPIPlugin {
            schemaFormat avro
            idType integer
            idTypeFormat int64
            avroPackage com.arcadiaeditions.payments.processing.events.avro
            includeCloudEventsHeaders true
            includeKafkaCommonHeaders true
            targetFile "./asyncapi.yml"
        }
        ZDLToAsyncAPIClientPlugin {
            id "urn:com.arcadiaeditions.payments.processing:client"
            title "Arcadia Editions - Payments Processing - AsyncAPI Client"
            targetFile "./asyncapi-client.yml"
        }
        /** Generates an OpenAPI 3.0 specification from this ZDL model. */
        ZDLToOpenAPIPlugin {
            idType integer
            idTypeFormat int64
            targetFile "./openapi.yml"
        }
    }
}

apis {
    asyncapi client OrdersCheckoutApi "https://raw.githubusercontent.com/arcadia-editions/orders-checkout-api/main/asyncapi.yml"
    asyncapi client FulfillmentShippingApi "https://raw.githubusercontent.com/arcadia-editions/fulfillment-shipping-api/main/asyncapi.yml"
}

@aggregate
@lifecycle(field: status, initial: AUTHORIZATION_REQUESTED)
entity Payment {
    paymentId String required unique
    orderId String required unique
    customerId String required
    amount BigDecimal required
    currency String required
    status PaymentStatus required
    providerReference String
    attemptCount Integer required
    authorizedAt Instant
    declinedAt Instant
    failedAt Instant
    capturedAt Instant
    voidedAt Instant
    failureReason String
}

enum PaymentStatus {
    AUTHORIZATION_REQUESTED
    AUTHORIZED
    DECLINED
    FAILED
    RETRY_EXHAUSTED
    CAPTURED
    VOIDED
}

input AuthorizePaymentInput {
    orderId String required
    customerId String required
    amount BigDecimal required
    currency String required
    paymentMethodId String required
}

input RetryPaymentInput {
    paymentId String required
    orderId String required
}

input CapturePaymentInput {
    paymentId String required
    orderId String required
}

input VoidPaymentInput {
    paymentId String required
    orderId String required
    reason String required
}

@rest("/payments")
service PaymentsProcessingService for (Payment) {

    @asyncapi(api: OrdersCheckoutApi, channel: OrderCreatedChannel)
    @transition(to: AUTHORIZATION_REQUESTED)
    authorizePayment(AuthorizePaymentInput) Payment withEvents PaymentAuthorized PaymentDeclined PaymentFailed

    @asyncapi(channel: PaymentFailedChannel)
    @transition(from: FAILED, to: AUTHORIZATION_REQUESTED)
    retryPayment(RetryPaymentInput) Payment withEvents PaymentRetried PaymentRetryExhausted

    @asyncapi(api: FulfillmentShippingApi, channel: FulfillmentScheduledChannel)
    @transition(from: AUTHORIZED, to: CAPTURED)
    capturePayment(CapturePaymentInput) Payment withEvents PaymentCaptured PaymentCaptureFailed

    @asyncapi(api: FulfillmentShippingApi, channel: FulfillmentFailedChannel)
    @asyncapi(channel: PaymentCaptureFailedChannel)
    @transition(from: [AUTHORIZED, AUTHORIZATION_REQUESTED, FAILED], to: VOIDED)
    voidPayment(VoidPaymentInput) Payment withEvents PaymentVoided
}

@asyncapi({ channel: "PaymentAuthorizedChannel", topic: "payments-processing.events.payment-authorized" })
event PaymentAuthorized {
    paymentId String
    orderId String
    customerId String
    amount BigDecimal
    currency String
    providerReference String
    authorizedAt Instant
}

@asyncapi({ channel: "PaymentDeclinedChannel", topic: "payments-processing.events.payment-declined" })
event PaymentDeclined {
    paymentId String
    orderId String
    customerId String
    amount BigDecimal
    currency String
    reason String
    declinedAt Instant
}

@asyncapi({ channel: "PaymentFailedChannel", topic: "payments-processing.events.payment-failed" })
event PaymentFailed {
    paymentId String
    orderId String
    customerId String
    attemptCount Integer
    reason String
    failedAt Instant
}

@asyncapi({ channel: "PaymentRetriedChannel", topic: "payments-processing.events.payment-retried" })
event PaymentRetried {
    paymentId String
    orderId String
    customerId String
    attemptCount Integer
    retriedAt Instant
}

@asyncapi({ channel: "PaymentRetryExhaustedChannel", topic: "payments-processing.events.payment-retry-exhausted" })
event PaymentRetryExhausted {
    paymentId String
    orderId String
    customerId String
    attemptCount Integer
    reason String
    exhaustedAt Instant
}

@asyncapi({ channel: "PaymentCapturedChannel", topic: "payments-processing.events.payment-captured" })
event PaymentCaptured {
    paymentId String
    orderId String
    customerId String
    amount BigDecimal
    currency String
    providerReference String
    capturedAt Instant
}

@asyncapi({ channel: "PaymentCaptureFailedChannel", topic: "payments-processing.events.payment-capture-failed" })
event PaymentCaptureFailed {
    paymentId String
    orderId String
    customerId String
    reason String
    failedAt Instant
}

@asyncapi({ channel: "PaymentVoidedChannel", topic: "payments-processing.events.payment-voided" })
event PaymentVoided {
    paymentId String
    orderId String
    customerId String
    providerReference String
    reason String
    voidedAt Instant
}
